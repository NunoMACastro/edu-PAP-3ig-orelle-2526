# Copia este ficheiro para `.env.local` apenas quando quiseres usar a OpenAI real.
# A chave é secreta: nunca a coloques no frontend nem a publiques no repositório.
ORELLE_LOCAL_OPENAI_ENABLED=false
OPENAI_API_KEY=
